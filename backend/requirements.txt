# Render: Python 3.11.9 required (runtime.txt). Build via: bash build.sh
# torch is installed CPU-only in build.sh to avoid CUDA deps on Render.
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

numpy>=1.24.0,<2
scikit-learn>=1.3.0,<1.6.0
faiss-cpu>=1.7.4,<1.10.0

# Pinned ML stack compatible with torch 2.1.2 (transformers 5.x needs torch >= 2.4).
huggingface-hub>=0.23.0,<0.26.0
transformers>=4.41.0,<5.0.0
sentence-transformers>=2.7.0,<3.0.0

httpx
python-multipart
pyyaml
tqdm
python-dotenv

# Production hardening
slowapi>=0.1.9
python-json-logger>=2.0
